#ifndef _CONVERTING_H_
#define _CONVERTING_H_
// Include standard libraries for input/output and string-to-number conversion.
// These libraries provide necessary functions for converting string representations of numbers
// into their numeric forms.
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Declare the converting function.
// This function demonstrates converting strings into various numeric types (int, double, long).
void converting(void);

#endif 
